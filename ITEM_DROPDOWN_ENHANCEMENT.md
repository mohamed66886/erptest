# تحسين قائمة اختيار الأصناف - Item Dropdown Enhancement

## التاريخ: 5 أكتوبر 2025

## الملخص
تم تحسين قائمة اختيار الأصناف في صفحة إضافة فاتورة المبيعات (`sales.tsx`) لتوفير معلومات أكثر وضوحاً حول الأصناف، بما في ذلك الرصيد المتاح في المخزن وحالة الصنف.

## التحسينات المنفذة

### 1. إزالة التكرارات
- تم تعديل القائمة لعرض كل صنف مرة واحدة فقط
- استخدام `reduce` لتصفية الأصناف المكررة بناءً على اسم الصنف

### 2. عرض الرصيد المتاح
- إضافة دالة `calculateItemStock` لحساب الرصيد المتاح لكل صنف في المخزن المحدد
- حساب الرصيد بناءً على:
  - **الوارد**: المشتريات + مرتجعات المبيعات
  - **المنصرف**: المبيعات
  - **الرصيد النهائي**: الوارد - المنصرف

### 3. عرض حالة الصنف
تم إضافة مؤشرات بصرية ملونة لحالة كل صنف:

#### حالات الصنف:
- 🟢 **متوفر** (أخضر): الرصيد أكبر من 10 قطع
- 🟡 **كمية قليلة** (برتقالي): الرصيد بين 1 و 10 قطع
- 🔴 **نفذ من المخزون** (أحمر): الرصيد = 0
- 🔴 **رصيد سالب** (أحمر داكن): الرصيد أقل من 0
- 🔴 **موقوف مؤقتاً** (أحمر): الصنف موقوف مؤقتاً (tempCodes = true)
- ⚪ **اختر المخزن أولاً** (رمادي): لم يتم اختيار مخزن بعد

### 4. معلومات إضافية في القائمة
لكل صنف في القائمة المنسدلة، يتم عرض:
- اسم الصنف
- كود الصنف (SKU)
- سعر البيع
- **الرصيد المتاح** (بناءً على المخزن المحدد)
- **حالة الصنف** (بشارة ملونة)

### 5. دعم المخازن المتعددة
- في وضع "مخزن واحد": يتم حساب الرصيد بناءً على المخزن الرئيسي للفاتورة
- في وضع "مخازن متعددة": يتم تحديث الرصيد عند اختيار مخزن لكل صنف

### 6. تحميل تلقائي للأرصدة
- عند اختيار المخزن، يتم تحميل أرصدة جميع الأصناف تلقائياً
- مؤشر تحميل يظهر أثناء جلب البيانات
- تأخير 300ms لتجنب التحميل المفرط عند تغيير المخزن بسرعة

## التعديلات التقنية

### المتغيرات الجديدة
```typescript
const [itemStocks, setItemStocks] = useState<{[key: string]: number}>({});
const [loadingStocks, setLoadingStocks] = useState(false);
```

### الدوال الجديدة
```typescript
// حساب الرصيد المتاح لصنف في مخزن محدد
const calculateItemStock = useCallback(async (itemName: string, warehouseId: string): Promise<number>
```

### الـ useEffect الجديدة
1. **تحميل أرصدة جميع الأصناف عند تغيير المخزن الرئيسي**
2. **تحديث رصيد صنف محدد في وضع المخازن المتعددة**

## الفوائد

### للمستخدم
- ✅ معرفة فورية لتوفر الصنف قبل إضافته للفاتورة
- ✅ تجنب إضافة أصناف غير متوفرة في المخزون
- ✅ رؤية واضحة لحالة كل صنف (متوفر، قليل، منتهي، موقوف)
- ✅ عدم ظهور نفس الصنف مكرراً في القائمة

### للنظام
- ✅ تحسين تجربة المستخدم
- ✅ تقليل الأخطاء في الفواتير
- ✅ حساب دقيق للمخزون في الوقت الفعلي
- ✅ دعم كامل للمخازن المتعددة

## ملاحظات الأداء
- يتم حساب الأرصدة مرة واحدة عند اختيار المخزن
- استخدام Promise.all لتحميل الأرصدة بشكل متوازي
- تأخير 300ms لتحسين الأداء عند التبديل السريع بين المخازن
- لا يتم إعادة حساب الأرصدة إلا عند تغيير المخزن أو قائمة الأصناف

## التوافق
- ✅ متوافق مع وضع المخزن الواحد
- ✅ متوافق مع وضع المخازن المتعددة
- ✅ يعمل مع جميع أنواع الفواتير
- ✅ يدعم جميع المتصفحات الحديثة

## الملفات المعدلة
- `/src/pages/stores/sales.tsx`

## الاختبارات الموصى بها
1. اختيار مخزن والتحقق من ظهور الأرصدة
2. إضافة صنف متوفر والتحقق من الرصيد
3. إضافة صنف غير متوفر (رصيد = 0)
4. التبديل بين المخازن والتحقق من تحديث الأرصدة
5. اختبار وضع المخازن المتعددة
6. التحقق من عدم ظهور أصناف مكررة

## مثال على الاستخدام

عند اختيار صنف من القائمة المنسدلة، سيظهر:

```
اسم الصنف: لابتوب HP ProBook          [متوفر]
كود: LP-001    السعر: 2500 ر.س    الرصيد: 15.00
```

أو في حالة نفاذ الصنف:

```
اسم الصنف: ماوس لوجيتك          [نفذ من المخزون]
كود: MS-005    السعر: 50 ر.س    الرصيد: 0.00
```

أو في حالة الصنف الموقوف:

```
اسم الصنف: كيبورد قديم          [موقوف مؤقتاً]
كود: KB-OLD    السعر: 100 ر.س
```

---

**تم التطوير بواسطة:** GitHub Copilot  
**التاريخ:** 5 أكتوبر 2025
