# نظام الصلاحيات - دليل الاستخدام

## 📋 نظرة عامة

تم تطوير نظام صلاحيات متكامل لإدارة وصول المستخدمين إلى صفحات إدارة المخرجات في النظام.

## 🔐 المستخدمون والأدوار

### أنواع المستخدمين:

1. **مدير عام**
   - لديه صلاحية الوصول الكامل لجميع الصفحات
   - لا يحتاج إلى تحديد صلاحيات محددة
   - يمكنه إدارة جميع المستخدمين

2. **مدير فرع**
   - يجب تحديد الفرع المسؤول عنه
   - يحتاج إلى صلاحيات محددة للوصول للصفحات
   - محدود بالصلاحيات الممنوحة له

3. **مدير مستودع**
   - يجب تحديد مستودع التوصيل المسؤول عنه
   - يحتاج إلى صلاحيات محددة للوصول للصفحات
   - محدود بالصلاحيات الممنوحة له

## 📄 الصفحات المحمية

### الإعدادات:
- `governorates` - إدارة المحافظات
- `regions` - إدارة المناطق
- `districts` - إدارة الأحياء
- `drivers` - إدارة السائقين
- `branch-status` - حالة الفرع
- `delivery-warehouses` - مستودعات التوصيل
- `link-branches` - ربط الفروع
- `delivery-settings` - إعدادات التوصيل
- `users` - إدارة المستخدمين

### العمليات:
- `delivery-orders` - الطلبات
- `confirm-orders` - تأكيد الطلبات
- `completed-orders` - الطلبات المكتملة
- `archived-orders` - الطلبات المؤرشفة

### التقارير:
- `comprehensive-reports` - التقارير الشاملة

## 🚀 كيفية العمل

### 1. إضافة مستخدم جديد

```
1. اذهب إلى: إدارة المخرجات > الإعدادات > إدارة المستخدمين
2. اضغط على "إضافة مستخدم جديد"
3. املأ البيانات المطلوبة:
   - اسم المستخدم
   - الاسم الكامل
   - كلمة المرور
   - المنصب (مدير عام / مدير فرع / مدير مستودع)
   - الفرع (إذا كان مدير فرع)
   - المستودع (إذا كان مدير مستودع)
4. حدد الصلاحيات المطلوبة من قائمة الصفحات
5. احفظ المستخدم
```

### 2. تسجيل الدخول

```
1. في صفحة تسجيل الدخول، اختر اسم المستخدم من القائمة المنسدلة
2. أدخل كلمة المرور
3. اضغط "تسجيل الدخول"
```

### 3. التحكم في الوصول

- **الكروت المجمدة**: في صفحة إدارة المخرجات، الكروت التي لا تملك صلاحية الوصول إليها ستكون:
  - باهتة اللون (opacity: 50%)
  - رمادية (grayscale)
  - غير قابلة للنقر
  - تظهر رسالة تنبيه عند المرور عليها

- **حماية الروت**: إذا حاول المستخدم الوصول مباشرة عبر URL لصفحة ليس لديه صلاحية لها:
  - يتم عرض صفحة "غير مصرح بالدخول"
  - رسالة واضحة بأنه لا يملك الصلاحية
  - زر للعودة للخلف

## 🛠️ الملفات المهمة

### 1. `usePermissions.ts`
Hook مخصص لإدارة الصلاحيات:
```typescript
const { hasPermission, currentUser, permissions } = usePermissions();

// للتحقق من صلاحية
if (hasPermission('governorates')) {
  // المستخدم لديه صلاحية
}
```

### 2. `ProtectedRoute.tsx`
مكون لحماية الصفحات:
```typescript
<ProtectedRoute requiredPermission="governorates">
  <Governorates />
</ProtectedRoute>
```

### 3. `UsersManagement.tsx`
صفحة إدارة المستخدمين والصلاحيات

### 4. `OutputsManagement.tsx`
صفحة عرض الكروت مع دعم تجميد الكروت غير المصرح بها

## 💾 تخزين البيانات

- **Firestore Collection**: `users`
  - `id`: معرف المستخدم
  - `username`: اسم المستخدم
  - `fullName`: الاسم الكامل
  - `password`: كلمة المرور
  - `position`: المنصب
  - `branchId`: معرف الفرع (للمدير الفرع)
  - `branchName`: اسم الفرع
  - `warehouseId`: معرف المستودع (لمدير المستودع)
  - `warehouseName`: اسم المستودع
  - `permissions`: مصفوفة من معرفات الصلاحيات
  - `createdAt`: تاريخ الإنشاء
  - `updatedAt`: تاريخ آخر تحديث

- **localStorage**: `currentUser`
  - يتم تخزين بيانات المستخدم الحالي بعد تسجيل الدخول
  - يتم تحديثها عند تعديل بيانات المستخدم

## 🔄 تحديث الصلاحيات

عند تعديل صلاحيات المستخدم:
1. يتم تحديث قاعدة البيانات
2. إذا كان المستخدم المعدل هو المستخدم الحالي:
   - يتم تحديث localStorage
   - يظهر تنبيه بضرورة إعادة تحميل الصفحة
   - يُنصح بإعادة تسجيل الدخول لتفعيل التغييرات

## ⚠️ ملاحظات مهمة

1. **المدير العام**: لديه صلاحيات كاملة تلقائياً، لا حاجة لتحديد صلاحيات له
2. **الأمان**: النظام الحالي يستخدم client-side validation، للإنتاج يُنصح بإضافة Firebase Authentication
3. **كلمات المرور**: يتم تخزينها كنص عادي حالياً، يجب تشفيرها في الإنتاج
4. **تحديث الجلسة**: بعد تعديل الصلاحيات، يجب إعادة تحميل الصفحة أو إعادة تسجيل الدخول

## 🎯 مثال عملي

### إنشاء مدير فرع مع صلاحيات محددة:

```
اسم المستخدم: ahmad_branch
الاسم الكامل: أحمد محمد - مدير الفرع الرئيسي
المنصب: مدير فرع
الفرع: الفرع الرئيسي
الصلاحيات:
  ✅ إدارة المحافظات
  ✅ إدارة المناطق
  ✅ الطلبات
  ✅ تأكيد الطلبات
  ❌ إدارة المستخدمين (غير مصرح)
  ❌ إعدادات التوصيل (غير مصرح)
```

عند تسجيل دخول هذا المستخدم:
- سيرى الكروت المصرح بها بشكل طبيعي
- الكروت غير المصرح بها ستكون مجمدة
- لن يتمكن من الوصول للصفحات عبر URL مباشرة

## 📞 الدعم

في حالة وجود أي مشاكل أو استفسارات، يرجى التواصل مع فريق التطوير.

---
**تاريخ آخر تحديث**: نوفمبر 2025
**الإصدار**: 1.0.0
