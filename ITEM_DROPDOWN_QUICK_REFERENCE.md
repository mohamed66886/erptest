# دليل مرجعي سريع - Item Dropdown Enhancement Quick Reference

## الاستخدام السريع

### للمطورين

#### إضافة نفس الميزة لصفحة أخرى:

```typescript
// 1. إضافة المتغيرات
const [itemStocks, setItemStocks] = useState<{[key: string]: number}>({});
const [loadingStocks, setLoadingStocks] = useState(false);

// 2. إضافة دالة حساب المخزون
const calculateItemStock = useCallback(async (itemName: string, warehouseId: string): Promise<number> => {
  if (!itemName || !warehouseId) return 0;
  
  try {
    // جلب المشتريات والمبيعات والمرتجعات
    const purchasesSnap = await getDocs(collection(db, "purchases_invoices"));
    const salesSnap = await getDocs(collection(db, "sales_invoices"));
    const salesReturnsSnap = await getDocs(collection(db, "sales_returns"));
    
    // حساب الوارد والمنصرف
    let totalIncoming = 0;
    let totalOutgoing = 0;
    
    // ... (راجع الكود الكامل في sales.tsx)
    
    return totalIncoming - totalOutgoing;
  } catch (error) {
    console.error('خطأ في حساب المخزون:', error);
    return 0;
  }
}, []);

// 3. إضافة useEffect لتحميل الأرصدة
useEffect(() => {
  const fetchAllItemStocks = async () => {
    if (!selectedWarehouse || items.length === 0) return;
    
    setLoadingStocks(true);
    const stocks: {[key: string]: number} = {};
    
    try {
      await Promise.all(
        items.map(async (item) => {
          const stock = await calculateItemStock(item.name, selectedWarehouse);
          stocks[item.name] = stock;
        })
      );
      setItemStocks(stocks);
    } finally {
      setLoadingStocks(false);
    }
  };
  
  const timeoutId = setTimeout(() => fetchAllItemStocks(), 300);
  return () => clearTimeout(timeoutId);
}, [selectedWarehouse, items, calculateItemStock]);

// 4. تحديث القائمة المنسدلة
<Select
  loading={loadingStocks}
  placeholder={loadingStocks ? "جاري تحميل الأرصدة..." : "اختر الصنف"}
  options={items
    .reduce((unique, item) => {
      if (!unique.find(u => u.name === item.name)) unique.push(item);
      return unique;
    }, [])
    .map(item => ({
      label: item.name,
      value: item.name,
      disabled: item.tempCodes
    }))
  }
  optionRender={(option) => {
    const item = items.find(i => i.name === option.value);
    const stock = itemStocks[item?.name || ''] ?? null;
    
    // تحديد الحالة
    let status = '';
    let color = '';
    
    if (item?.tempCodes) {
      status = 'موقوف مؤقتاً';
      color = '#ff4d4f';
    } else if (stock !== null) {
      if (stock > 10) { status = 'متوفر'; color = '#52c41a'; }
      else if (stock > 0) { status = 'كمية قليلة'; color = '#faad14'; }
      else if (stock === 0) { status = 'نفذ من المخزون'; color = '#ff4d4f'; }
      else { status = 'رصيد سالب'; color = '#cf1322'; }
    }
    
    return (
      <div>
        <div>
          <span>{item?.name}</span>
          {status && <span style={{color}}>{status}</span>}
        </div>
        <div>
          {item?.itemCode && <span>كود: {item.itemCode}</span>}
          {item?.salePrice && <span>السعر: {item.salePrice}</span>}
          {stock !== null && <span>الرصيد: {stock.toFixed(2)}</span>}
        </div>
      </div>
    );
  }}
/>
```

---

## للمستخدمين

### كيفية استخدام القائمة المحسنة

#### 1️⃣ اختر المخزن أولاً
```
الفرع: [الفرع الرئيسي]
المخزن: [المخزن الرئيسي] ← اختر هذا أولاً
```

#### 2️⃣ افتح قائمة الأصناف
```
اسم الصنف: [اضغط هنا] ▼
```

#### 3️⃣ ابحث عن الصنف
```
🔍 ابحث بالاسم أو الكود...
```

#### 4️⃣ اقرأ المعلومات
```
لابتوب HP ProBook    [🟢 متوفر]
كود: LP-001  │  السعر: 2500 ر.س  │  الرصيد: 15.00
                                        ↑
                            هذا هو الرصيد المتاح
```

#### 5️⃣ اختر الصنف
```
✓ تم تحديد الصنف: لابتوب HP ProBook
```

---

## الأسئلة الشائعة (FAQ)

### Q1: لماذا لا تظهر الأرصدة؟
**A:** تأكد من اختيار المخزن أولاً. الأرصدة تُحسب بناءً على المخزن المحدد.

### Q2: ماذا يعني "جاري تحميل الأرصدة"؟
**A:** النظام يحسب أرصدة جميع الأصناف في المخزن المختار. انتظر قليلاً (عادة 2-3 ثواني).

### Q3: لماذا يظهر نفس الصنف مرة واحدة فقط الآن؟
**A:** تم تحسين النظام لإزالة التكرارات وعرض كل صنف مرة واحدة فقط.

### Q4: ما الفرق بين "نفذ من المخزون" و "رصيد سالب"؟
**A:** 
- **نفذ من المخزون** (0): لا يوجد كمية متاحة حالياً
- **رصيد سالب** (-): تم بيع كمية أكبر من المشتريات (يحتاج مراجعة)

### Q5: هل يمكن إضافة صنف "نفذ من المخزون"؟
**A:** نعم، إذا كان الصنف يسمح بالرصيد السالب (allowNegative = true). وإلا سيظهر تحذير.

### Q6: ماذا أفعل إذا كان الصنف "موقوف مؤقتاً"؟
**A:** لا يمكن إضافة الأصناف الموقوفة للفاتورة. تواصل مع مدير النظام لتفعيل الصنف.

### Q7: كيف أعرف إذا كان الصنف يحتاج إعادة طلب؟
**A:** ابحث عن حالة "🟡 كمية قليلة" - هذه الأصناف الرصيد بينها قليل (1-10 قطع).

### Q8: هل الأرصدة حقيقية ومحدثة؟
**A:** نعم، الأرصدة محسوبة في الوقت الفعلي من قاعدة البيانات عند كل مرة تفتح فيها القائمة.

---

## الاختصارات والنصائح

### ⌨️ اختصارات لوحة المفاتيح
- `↓` / `↑` : التنقل بين الأصناف
- `Enter` : اختيار الصنف
- `Esc` : إغلاق القائمة
- `Ctrl + F` : البحث السريع

### 💡 نصائح للاستخدام الأمثل
1. اختر المخزن قبل البحث عن الأصناف
2. استخدم البحث بالكود للعثور السريع على الصنف
3. انتبه لمؤشرات الألوان (أخضر = آمن، أحمر = تحذير)
4. تحقق من الرصيد قبل إدخال كميات كبيرة
5. إذا كان الصنف "كمية قليلة"، أخبر مدير المشتريات

---

## استكشاف الأخطاء

### المشكلة: القائمة فارغة
✅ **الحل:**
1. تأكد من وجود أصناف في النظام
2. تحقق من أن الأصناف من النوع "مستوى ثاني"
3. تحديث الصفحة (F5)

### المشكلة: لا تظهر الأرصدة
✅ **الحل:**
1. اختر المخزن أولاً
2. انتظر انتهاء التحميل
3. تحقق من الاتصال بالإنترنت

### المشكلة: الأرصدة غير صحيحة
✅ **الحل:**
1. تحقق من صحة فواتير المشتريات والمبيعات
2. راجع المرتجعات
3. تواصل مع مدير النظام للتدقيق

### المشكلة: بطء في التحميل
✅ **الحل:**
1. طبيعي إذا كان عدد الأصناف كبير (> 1000)
2. النظام يستخدم تحميل متوازي (Promise.all)
3. الأرصدة تُحمّل مرة واحدة فقط عند اختيار المخزن

---

## معلومات تقنية

### قاعدة البيانات
```
Collections المستخدمة:
├── inventory_items (الأصناف)
├── purchases_invoices (فواتير المشتريات)
├── sales_invoices (فواتير المبيعات)
└── sales_returns (مرتجعات المبيعات)
```

### معادلة حساب الرصيد
```
الرصيد المتاح = (المشتريات + مرتجعات المبيعات) - المبيعات

حيث:
- المشتريات: جميع الكميات من purchases_invoices للمخزن المحدد
- مرتجعات المبيعات: الكميات المرتجعة من sales_returns
- المبيعات: جميع الكميات من sales_invoices للمخزن المحدد
```

### حالات الصنف (Status Logic)
```typescript
if (tempCodes) → "موقوف مؤقتاً"
else if (stock > 10) → "متوفر"
else if (stock > 0 && stock <= 10) → "كمية قليلة"
else if (stock === 0) → "نفذ من المخزون"
else if (stock < 0) → "رصيد سالب"
else → "اختر المخزن أولاً"
```

---

## جهات الاتصال للدعم

**للمطورين:**
- راجع الملف: `/src/pages/stores/sales.tsx`
- السطور: 1752-1850 (دوال حساب المخزون)
- السطور: 2620-2750 (عرض القائمة المنسدلة)

**للمستخدمين:**
- تواصل مع الدعم الفني
- أرسل لقطة شاشة للمشكلة
- اذكر المخزن والصنف المتأثر

---

**آخر تحديث:** 5 أكتوبر 2025  
**الإصدار:** 1.0  
**الحالة:** ✅ نشط ومُختبر
