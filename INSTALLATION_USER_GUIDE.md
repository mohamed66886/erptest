# 🔧 دليل إضافة مستخدم تركيب

## ✅ تم إصلاح المشاكل التالية:

### 1. تصحيح مسارات قاعدة البيانات
- ✅ تم تغيير `financial_years` إلى `financialYears` في جميع الملفات
- ✅ تم تحديث `LoginPage.tsx`
- ✅ تم تحديث `UsersManagement.tsx`

### 2. تصحيح منطق فحص السنوات النشطة
- ✅ يدعم الآن `status: 'مفتوحة'` و `activeStatus: 'نشطة'`

## 📊 حالة قاعدة البيانات الحالية:

```
✅ السنوات المالية: 3 سنوات
  - 2024 (id: 7yEfe3dFL5i6qUVm1V87) - status: 'مغلقة', activeStatus: 'نشطة'
  - 2018 (id: kbEIrI9McVnDtACAxgxt) - status: 'مفتوحة'
  - 2025 (id: uwPhx23jLXWvbDNgZPDL) - status: 'مفتوحة', activeStatus: 'نشطة'

✅ مستخدمو التوصيل: 17 مستخدم
❌ مستخدمو التركيب: 0 مستخدم (لا يوجد!)
```

## 🎯 الحل: إضافة مستخدمي تركيب

### الطريقة 1: من خلال واجهة النظام (الأسهل) ⭐

1. **سجل دخول بحساب لديه صلاحيات**

2. **اذهب إلى إدارة مستخدمي التركيب:**
   - المسار: `/installation/users`
   - أو من القائمة: `الإعدادات` > `إدارة المستخدمين`

3. **اضغط على زر "إضافة مستخدم جديد"**

4. **املأ البيانات التالية:**

   ```
   اسم المستخدم: installation_admin
   الاسم الكامل: مدير التركيب
   كلمة المرور: 123456
   المنصب: مدير عام
   نوع الوصول: installation
   الصلاحيات: (اختر جميع الصفحات)
   ```

5. **مثال لمستخدم آخر:**

   ```
   اسم المستخدم: installer1
   الاسم الكامل: أحمد محمد - فني تركيب
   كلمة المرور: 123456
   المنصب: فني
   نوع الوصول: installation
   الصلاحيات: 
     - الطلبات
     - الطلبات المؤكدة
     - الطلبات المكتملة
   ```

### الطريقة 2: إضافة يدوياً من Firebase Console

1. **افتح Firebase Console**
   - اذهب إلى Firestore Database

2. **انتقل إلى المسار:**
   ```
   financialYears/{yearId}/installation_users
   ```
   حيث `{yearId}` هو أحد:
   - `uwPhx23jLXWvbDNgZPDL` (سنة 2025)
   - `7yEfe3dFL5i6qUVm1V87` (سنة 2024)

3. **أضف مستند جديد بالبيانات التالية:**

   ```json
   {
     "username": "installation_admin",
     "fullName": "مدير التركيب",
     "password": "123456",
     "position": "مدير عام",
     "accessType": "installation",
     "permissions": [
       "installation-settings",
       "technicians",
       "users-management",
       "installation-orders",
       "installation-confirmed-orders",
       "installation-completed-orders",
       "installation-archived-orders"
     ],
     "createdAt": {Firebase Timestamp - الآن},
     "updatedAt": {Firebase Timestamp - الآن}
   }
   ```

## 📋 قائمة الصلاحيات المتاحة

### صلاحيات التركيب:
- `installation-settings` - إعدادات التركيب
- `technicians` - إدارة الفنيين
- `users-management` - إدارة المستخدمين
- `installation-orders` - الطلبات
- `installation-confirmed-orders` - الطلبات المؤكدة
- `installation-completed-orders` - الطلبات المكتملة
- `installation-archived-orders` - الطلبات المؤرشفة

### صلاحيات التوصيل (لنوع installation_delivery):
- `delivery-orders` - طلبات التوصيل
- `confirm-orders` - تأكيد طلبات التوصيل
- `completed-orders` - طلبات التوصيل المكتملة
- `drivers` - إدارة السائقين
- ... وغيرها

## 🧪 اختبار النظام

1. **بعد إضافة مستخدم تركيب:**
   - قم بتحديث صفحة تسجيل الدخول (F5)
   - يجب أن يظهر المستخدم في القائمة المنسدلة
   - يجب أن يظهر badge "تركيب" 🔧 بجانب الاسم
   - يجب أن يظهر رقم السنة المالية

2. **سجل الدخول بحساب التركيب:**
   - اختر المستخدم من القائمة
   - أدخل كلمة المرور
   - اضغط "تسجيل الدخول"

3. **تحقق من الصلاحيات:**
   - يجب أن تظهر فقط الصفحات المسموح بها في القائمة الجانبية
   - جرب الوصول إلى صفحة غير مسموح بها (يجب أن يتم الرفض)

## 🔍 تشخيص المشاكل

### إذا لم يظهر المستخدم في القائمة:

1. **تحقق من Console في المتصفح:**
   ```
   يجب أن تشاهد:
   ✅ 📊 Found X installation users in year 2025
   ```

2. **تحقق من المسار في Firestore:**
   ```
   financialYears/uwPhx23jLXWvbDNgZPDL/installation_users
   ```
   (تأكد أن اسم المجموعة `financialYears` وليس `financial_years`)

3. **تحقق من حالة السنة المالية:**
   - يجب أن تكون `status: 'مفتوحة'` أو `activeStatus: 'نشطة'`

### إذا ظهرت أخطاء في تسجيل الدخول:

1. **تحقق من كلمة المرور:**
   - كلمة المرور تُحفظ كنص عادي (plain text)
   - يجب أن تتطابق تماماً

2. **تحقق من localStorage:**
   ```javascript
   console.log(localStorage.getItem('currentUser'))
   ```
   - يجب أن يحتوي على `userType: 'installation'`

## 📝 ملاحظات مهمة

1. **تمييز نوع المستخدم:**
   - مستخدمو التوصيل: badge بنفسجي 🚚
   - مستخدمو التركيب: badge كهرماني 🔧

2. **السنوات المالية:**
   - كل مستخدم تركيب مرتبط بسنة مالية محددة
   - يظهر رقم السنة بجانب الاسم في القائمة

3. **نوع الوصول:**
   - `installation`: وصول لصفحات التركيب فقط
   - `installation_delivery`: وصول لصفحات التركيب والتوصيل معاً

## ✅ الخطوة التالية

**الآن قم بإضافة مستخدم تركيب تجريبي من خلال أحد الطرق أعلاه، ثم:**

1. حدّث صفحة تسجيل الدخول
2. يجب أن يظهر المستخدم الجديد في القائمة
3. جرب تسجيل الدخول به

---

**آخر تحديث:** تم إصلاح جميع مسارات قاعدة البيانات وتحديث منطق فحص السنوات النشطة.
