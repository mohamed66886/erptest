# ميزة إرسال PDF عبر الواتساب - إشعارات السائقين

## نظرة عامة
تم إضافة ميزة جديدة لإشعارات السائقين تتيح توليد ملف PDF تلقائياً ورفعه على Firebase Storage وإرسال رابط التحميل عبر الواتساب.

## المكتبات المستخدمة
- **jsPDF**: لتوليد ملفات PDF
- **html2canvas**: لتحويل HTML إلى صورة ثم إلى PDF
- **canvg**: مكتبة مساعدة لـ jsPDF للتعامل مع SVG
- **Firebase Storage**: لرفع وتخزين ملفات PDF

## الميزات الجديدة

### 1. زر إرسال واتساب مع PDF
- عند الضغط على زر "واتساب"، يتم:
  1. إنشاء ملف PDF يحتوي على تفاصيل طلبات السائق
  2. رفع الملف تلقائياً على Firebase Storage في مجلد `driver-orders/`
  3. الحصول على رابط التحميل المباشر
  4. إرسال رسالة واتساب تحتوي على:
     - تحية للسائق
     - عدد الطلبات
     - رابط تحميل PDF
     - تفاصيل الطلبات نصياً
     - رسالة ختامية

### 2. زر تحميل PDF
- زر منفصل لتحميل PDF مباشرة على جهاز المستخدم
- بدون رفع على Firebase Storage
- مفيد للطباعة أو الحفظ المحلي

### 3. معالجة أرقام الهواتف
- إضافة كود الدولة السعودية (+966) تلقائياً
- إزالة الصفر من البداية إذا كان موجوداً
- أمثلة:
  - `0501417237` → `966501417237`
  - `501417237` → `966501417237`
  - `+966501417237` → `966501417237`

## محتوى ملف PDF

يحتوي ملف PDF على:
1. **رأس الشركة**:
   - الاسم بالعربي والإنجليزي
   - السجل التجاري
   - الملف الضريبي
   - العنوان
   - الهواتف
   - الشعار

2. **معلومات السائق**:
   - الاسم
   - رقم الهاتف
   - عدد الطلبات
   - تاريخ ووقت الطباعة

3. **جدول الطلبات**:
   - رقم الفاتورة
   - اسم العميل
   - هاتف العميل
   - الحي والمنطقة
   - الملاحظات
   - حالة التركيب
   - حالة التوصيل
   - تاريخ التسليم

4. **قسم التوقيعات**:
   - توقيع السائق
   - ختم الشركة
   - توقيع مشرف التوصيل
   - التاريخ

## التخزين على Firebase

### مسار التخزين
```
driver-orders/{driverId}_{timestamp}.pdf
```

### مثال
```
driver-orders/ABC123_1699876543210.pdf
```

### الأمان
- الملفات مخزنة على Firebase Storage
- رابط التحميل يعمل مباشرة (public URL)
- يمكن تعديل صلاحيات Firebase Storage للتحكم في الوصول

## رسالة الواتساب

### نموذج الرسالة
```
السلام عليكم [اسم السائق]

لديك [عدد] طلب توصيل جديد قيد التنفيذ:

📄 رابط تحميل الطلبات (PDF):
[رابط Firebase Storage]

تفاصيل الطلبات:

1. فاتورة رقم: [رقم الفاتورة]
   - العميل: [اسم العميل]
   - الهاتف: [رقم الهاتف]
   - المنطقة: [الحي] [المنطقة]
   - ⚠️ يتطلب تركيب (إن وجد)
   - ملاحظات: [الملاحظات]

...

يرجى التواصل مع العملاء لتحديد مواعيد التوصيل.
شكراً لك 🙏
```

## التحسينات المستقبلية

يمكن إضافة:
1. خيار لحذف الملفات القديمة تلقائياً من Firebase Storage
2. إحصائيات عن الملفات المرسلة
3. إرسال PDF عبر البريد الإلكتروني
4. دعم لغات متعددة في PDF
5. تخصيص تصميم PDF حسب الفرع

## الاستخدام

1. اذهب إلى صفحة "إشعارات السائقين"
2. اختر السائق المطلوب
3. اضغط على زر "واتساب" لإرسال PDF عبر الواتساب
4. أو اضغط على زر "PDF" لتحميل الملف مباشرة

## ملاحظات مهمة

- يجب أن يكون رقم هاتف السائق متوفراً في قاعدة البيانات
- يجب أن تكون Firebase Storage مفعلة في المشروع
- تأكد من صلاحيات Firebase Storage Rules لرفع الملفات
- الملفات المرفوعة تبقى في Storage ما لم يتم حذفها يدوياً

## المتطلبات التقنية

```json
{
  "jspdf": "^2.5.2",
  "html2canvas": "^1.4.1",
  "canvg": "^4.0.2"
}
```

## Firebase Storage Rules

تأكد من إضافة القواعد التالية في Firebase Console:

```javascript
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /driver-orders/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
```

---

تم التطوير بواسطة GitHub Copilot ✨
تاريخ: 10 نوفمبر 2025
