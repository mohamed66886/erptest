# اختبار إضافة مستخدم تركيب

## المشكلة
مستخدمي التركيب لا يظهرون في صفحة تسجيل الدخول

## التحقق من البيانات

### 1. افتح Console في المتصفح (F12)
ستجد رسائل مثل:
```
🔍 Checking financial year: 2024 (xxx)
📋 Found 0 installation users in year 2024
```

### 2. تحقق من وجود مستخدمين في قاعدة البيانات

افتح Firebase Console وتحقق من:
```
financial_years/{yearId}/installation_users
```

### 3. إضافة مستخدم تجريبي عبر صفحة إدارة المستخدمين

1. افتح: http://localhost:8082/installation/users-management
2. اضغط "إضافة مستخدم جديد"
3. املأ البيانات:
   - اسم المستخدم: test_installation
   - الاسم الكامل: مستخدم تركيب تجريبي
   - كلمة المرور: 123456
   - المنصب: فني
   - نوع الوصول: تركيب فقط
   - اختر بعض الصلاحيات
4. احفظ

### 4. إعادة تحميل صفحة تسجيل الدخول

بعد إضافة المستخدم:
1. ارجع لصفحة تسجيل الدخول
2. ستجد المستخدم يظهر مع badge برتقالي "تركيب"

## حل بديل: إضافة مستخدم مباشرة في Firebase Console

إذا لم تعمل الصفحة، أضف مستخدم مباشرة في Firebase:

### المسار:
```
financial_years/{اختر السنة النشطة}/installation_users/{auto-id}
```

### البيانات:
```json
{
  "username": "test_installation",
  "fullName": "مستخدم تركيب تجريبي",
  "password": "123456",
  "position": "فني",
  "accessType": "installation",
  "permissions": ["installation-orders", "technicians"],
  "createdAt": "2024-11-15T10:00:00.000Z",
  "updatedAt": "2024-11-15T10:00:00.000Z"
}
```

### تحقق من activeStatus
تأكد أن السنة المالية لها:
```json
{
  "year": 2024,
  "activeStatus": "نشطة"  // ← مهم جداً
}
```

## التشخيص السريع في Console

نفذ هذا الكود في Console:
```javascript
// 1. جلب السنوات المالية
const db = firebase.firestore();
const years = await db.collection('financial_years').get();
console.log('السنوات المالية:', years.docs.map(d => ({
  id: d.id, 
  year: d.data().year, 
  status: d.data().activeStatus
})));

// 2. جلب مستخدمي سنة معينة
const yearId = 'YOUR_YEAR_ID'; // ضع ID السنة النشطة
const users = await db.collection(`financial_years/${yearId}/installation_users`).get();
console.log('مستخدمي التركيب:', users.docs.map(d => d.data()));
```
